#include <util.h>
#include <dozer.h>

void test(){
  FILE* input;
  FILE* output;
  input = fopen("datafile-defrag", "r");
  output = fopen("datafile-frag", "r");
  printf("Testing Root Block and Superblock...");
  char byte;
  while( n < BOOT_SIZE + )
  return 0;
}
